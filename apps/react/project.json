{
  "name": "react",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/react/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project react --web",
  "targets": {
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx prettier --config apps/react/.prettierrc --write apps/react/src/**/*.{ts,tsx,js,jsx,json,css,scss,md}"
      }
    }
  }
}
